alertmanager:
  persistentVolume:
    enabled: true
    storageClass: "local-path"

server:
  persistentVolume:
    enabled: true
    storageClass: "local-path"
  
  service:
    type: NodePort
    port: 80
